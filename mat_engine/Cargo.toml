[package]
name = "mat_engine"
version = "0.1.0"
authors = ["BuildTools <unconfigured@null.null>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = "0.4.8"
winit = "0.22.1"
image = "0.23.4"
futures = "0.3.4"

# TODO: Configure for other OS's.
[dependencies.wgpu]
version = "0.5.0"
features = ["vulkan"]
